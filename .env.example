# Copy this file to `.env` and adjust as needed.
# Defaults to a local SQLite file if unset - no setup required.
# Examples:
#   sqlite:///./orders.db
#   postgresql+psycopg2://user:password@localhost:5432/orders
DATABASE_URL=sqlite:///./orders.db
